# OpenAI Configuration
# API ключи хранятся в удалённом конфиге (nastia-data repo)
# Локально используем только proxy URLs
REACT_APP_OPENAI_PROXY_URL=https://nastia-openai-proxy.sergei-gubenov.workers.dev/v1/chat/completions

# Claude Configuration
# API ключи хранятся в удалённом конфиге (nastia-data repo)
# REACT_APP_CLAUDE_PROXY_URL=https://your-claude-proxy.workers.dev/v1/messages

# GitHub Pages
PUBLIC_URL=https://segigu.github.io/nastia-calendar
